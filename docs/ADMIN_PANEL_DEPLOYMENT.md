# 🔒 Безопасность и размещение админ-панели

## 📍 Варианты размещения

### Вариант 1: Отдельный поддомен ⭐ **РЕКОМЕНДУЕТСЯ**

**Структура:**
```
Основной сайт:  https://beauty-clinic.ru
Админ-панель:   https://admin.beauty-clinic.ru
```

**Преимущества:**
- ✅ Полная изоляция от основного сайта
- ✅ Можно закрыть доступ по IP-адресам
- ✅ Проще настроить защиту на уровне DNS/сервера
- ✅ Не видна в структуре основного сайта
- ✅ Можно использовать отдельный SSL сертификат

**Настройка на хостинге:**
1. Создать поддомен `admin` в панели хостинга
2. Настроить DNS запись
3. Загрузить админ-панель в отдельную директорию
4. Настроить защиту (см. раздел "Защита доступа")

---

### Вариант 2: Отдельная папка с защитой

**Структура:**
```
Основной сайт:  https://beauty-clinic.ru
Админ-панель:   https://beauty-clinic.ru/admin/
```

**Преимущества:**
- ✅ Не нужен поддомен
- ✅ Защита через `.htaccess` (Apache) или `nginx.conf` (Nginx)
- ✅ Проще для небольших проектов

**Недостатки:**
- ⚠️ Видна в URL (но можно скрыть через редирект)
- ⚠️ Менее изолирована

**Настройка защиты:**
```apache
# .htaccess в папке /admin/
# Запрет доступа без авторизации
<Files "*.php">
    Require all denied
    Require ip YOUR_IP_ADDRESS
</Files>
```

---

### Вариант 3: Локальное приложение (максимальная безопасность)

**Структура:**
```
Админ-панель работает только на вашем компьютере
Подключается к хостингу через FTP/SFTP API
```

**Преимущества:**
- ✅ Недоступна из интернета (максимальная безопасность)
- ✅ Нельзя взломать через веб-интерфейс
- ✅ Полный контроль

**Недостатки:**
- ⚠️ Нужен постоянный доступ к интернету
- ⚠️ Работает только с одного компьютера
- ⚠️ Нужно настроить FTP/SFTP доступ

**Реализация:**
- Админ-панель - это локальное приложение (Electron или веб-приложение)
- Подключается к хостингу через FTP/SFTP API
- Генерирует файлы локально и загружает на хостинг

---

## 🔐 Многоуровневая защита доступа

### Уровень 1: Ограничение доступа по IP-адресу

**Для одного пользователя (ваш случай):**

**Apache (.htaccess):**
```apache
# Разрешить доступ только с вашего IP
<RequireAll>
    Require ip YOUR_IP_ADDRESS
    Require ip YOUR_OFFICE_IP  # Если есть офис
</RequireAll>

# Или запретить всем, кроме вашего IP
Order Deny,Allow
Deny from all
Allow from YOUR_IP_ADDRESS
```

**Nginx (nginx.conf):**
```nginx
location /admin/ {
    allow YOUR_IP_ADDRESS;
    deny all;
    
    # Остальная конфигурация
}
```

**Как узнать свой IP:**
- Зайти на https://whatismyipaddress.com/
- Или использовать динамический IP (см. ниже)

**Проблема с динамическим IP:**
Если ваш IP меняется, используйте:
- VPN с фиксированным IP
- Или пропустите этот уровень (используйте только авторизацию)

---

### Уровень 2: Авторизация (логин/пароль)

**Обязательные меры:**

1. **Хеширование паролей:**
```php
// PHP пример
$hashedPassword = password_hash($password, PASSWORD_BCRYPT);
// Сохранить в БД

// Проверка
if (password_verify($inputPassword, $hashedPassword)) {
    // Авторизация успешна
}
```

2. **JWT токены или сессии:**
```php
// После успешной авторизации
session_start();
$_SESSION['admin_logged_in'] = true;
$_SESSION['admin_id'] = $adminId;
$_SESSION['expires'] = time() + 3600; // 1 час

// Проверка на каждой странице
if (!isset($_SESSION['admin_logged_in']) || $_SESSION['expires'] < time()) {
    header('Location: /admin/login.php');
    exit;
}
```

3. **Защита от CSRF:**
```php
// Генерация токена
$_SESSION['csrf_token'] = bin2hex(random_bytes(32));

// В форме
<input type="hidden" name="csrf_token" value="<?= $_SESSION['csrf_token'] ?>">

// Проверка при отправке
if ($_POST['csrf_token'] !== $_SESSION['csrf_token']) {
    die('CSRF attack detected!');
}
```

---

### Уровень 3: Дополнительные меры безопасности

1. **Rate Limiting (ограничение попыток входа):**
```php
// Максимум 5 попыток входа за 15 минут
// Блокировка IP после превышения
```

2. **Логирование всех действий:**
```php
// Логировать:
// - Попытки входа (успешные и неуспешные)
// - Все изменения (публикация статей, модерация отзывов)
// - IP адреса, время, действия
```

3. **Двухфакторная аутентификация (2FA) - опционально:**
- Google Authenticator
- SMS коды
- Email коды

4. **Автоматический выход:**
- После неактивности (30 минут)
- После закрытия браузера

---

## 🗄️ Структура базы данных для авторизации

```sql
CREATE TABLE admins (
    id INT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(50) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    email VARCHAR(100),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    last_login TIMESTAMP NULL,
    is_active BOOLEAN DEFAULT TRUE
);

CREATE TABLE admin_sessions (
    id INT PRIMARY KEY AUTO_INCREMENT,
    admin_id INT NOT NULL,
    session_token VARCHAR(255) UNIQUE NOT NULL,
    ip_address VARCHAR(45),
    user_agent TEXT,
    expires_at TIMESTAMP NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (admin_id) REFERENCES admins(id) ON DELETE CASCADE
);

CREATE TABLE admin_logs (
    id INT PRIMARY KEY AUTO_INCREMENT,
    admin_id INT NOT NULL,
    action VARCHAR(100) NOT NULL,
    details TEXT,
    ip_address VARCHAR(45),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (admin_id) REFERENCES admins(id)
);
```

---

## 📁 Структура файлов на хостинге

### Вариант 1: Поддомен

```
/
├── public_html/              # Основной сайт
│   ├── index.html
│   ├── html/
│   └── ...
│
└── admin.beauty-clinic.ru/   # Админ-панель (отдельная директория)
    ├── index.php             # Главная страница (проверяет авторизацию)
    ├── login.php             # Страница входа
    ├── dashboard.php         # Панель управления
    ├── reviews.php           # Модерация отзывов
    ├── articles.php          # Редактор статей
    ├── api/
    │   ├── auth.php          # API авторизации
    │   ├── reviews.php       # API отзывов
    │   └── articles.php      # API статей
    ├── config/
    │   ├── database.php      # Подключение к БД
    │   └── security.php     # Настройки безопасности
    └── .htaccess             # Защита доступа
```

### Вариант 2: Папка в основном сайте

```
public_html/
├── index.html                # Основной сайт
├── html/
│   └── ...
│
└── admin/                    # Админ-панель
    ├── .htaccess            # Защита доступа
    ├── index.php
    ├── login.php
    └── ...
```

---

## 🔧 Настройка защиты на хостинге Timeweb

### Шаг 1: Создание поддомена (если используете Вариант 1)

1. Зайти в панель управления Timeweb
2. Раздел "Домены" → "Поддомены"
3. Создать поддомен `admin`
4. Настроить директорию для поддомена

### Шаг 2: Настройка .htaccess

**Создать файл `/admin/.htaccess`:**

```apache
# Защита директории
<IfModule mod_authz_core.c>
    # Разрешить доступ только с вашего IP (замените на свой)
    Require ip YOUR_IP_ADDRESS
    
    # Или разрешить всем, но с авторизацией
    # Require all granted
</IfModule>

# Запретить просмотр файлов
Options -Indexes

# Защита конфигурационных файлов
<FilesMatch "\.(php|ini|log|sql)$">
    # Дополнительная защита
</FilesMatch>

# Защита от прямого доступа к API без авторизации
<FilesMatch "^api/.*\.php$">
    # Проверка авторизации через PHP
</FilesMatch>
```

### Шаг 3: Настройка PHP для безопасности

**Создать файл `/admin/config/security.php`:**

```php
<?php
// Настройки безопасности

// Отключить отображение ошибок в продакшене
ini_set('display_errors', 0);
error_reporting(0);

// Настройки сессий
ini_set('session.cookie_httponly', 1);
ini_set('session.cookie_secure', 1); // Только HTTPS
ini_set('session.use_strict_mode', 1);

// Защита от XSS
header('X-XSS-Protection: 1; mode=block');
header('X-Content-Type-Options: nosniff');
header('X-Frame-Options: DENY');

// CSP (Content Security Policy)
header("Content-Security-Policy: default-src 'self'");
```

---

## 🚀 Процесс входа в админ-панель

```
1. Пользователь заходит на https://admin.beauty-clinic.ru
   ↓
2. Проверка IP-адреса (.htaccess)
   ↓
3. Если IP разрешен → переход на login.php
   ↓
4. Ввод логина/пароля
   ↓
5. Проверка в БД (хешированный пароль)
   ↓
6. Создание сессии/JWT токена
   ↓
7. Редирект на dashboard.php
   ↓
8. На каждой странице проверка сессии
```

---

## 📝 Рекомендации для одного пользователя

**Минимальная настройка (простая, но безопасная):**

1. ✅ **Авторизация (логин/пароль)** - обязательно
2. ✅ **Хеширование паролей** - обязательно
3. ✅ **HTTPS** - обязательно (SSL сертификат)
4. ⚠️ **Ограничение по IP** - опционально (если IP статичный)
5. ✅ **Логирование действий** - рекомендуется
6. ✅ **Автоматический выход** - рекомендуется

**Расширенная настройка (максимальная безопасность):**

1. ✅ Все из минимальной настройки
2. ✅ Ограничение по IP (обязательно)
3. ✅ 2FA (двухфакторная аутентификация)
4. ✅ Rate limiting
5. ✅ Детальное логирование
6. ✅ Резервное копирование

---

## 🔄 Интеграция с основным сайтом

### Подход: Генерация статических файлов

**Как это работает:**

1. Админ входит в админ-панель (защищенную)
2. Создает/редактирует статью или модерацию отзыва
3. Нажимает "Опубликовать"
4. Админ-панель:
   - Генерирует HTML файл статьи
   - Обновляет JSON файлы (`data/articles.json`)
   - Загружает файлы на хостинг через FTP/SFTP
5. Основной сайт автоматически отображает новый контент

**Безопасность:**
- Админ-панель изолирована от основного сайта
- Основной сайт только читает файлы (не выполняет PHP)
- Нет прямого доступа к БД из основного сайта

---

## ✅ Чек-лист безопасности

- [ ] Админ-панель на отдельном поддомене или защищенной папке
- [ ] Настроен .htaccess с ограничением доступа (если возможно)
- [ ] Реализована авторизация (логин/пароль)
- [ ] Пароли хешируются (bcrypt)
- [ ] Используются сессии или JWT токены
- [ ] Защита от CSRF (токены)
- [ ] HTTPS включен (SSL сертификат)
- [ ] Логирование действий админа
- [ ] Автоматический выход после неактивности
- [ ] Валидация всех входных данных
- [ ] Санитизация HTML контента
- [ ] Проверка типов файлов при загрузке
- [ ] Резервное копирование данных

---

## 📚 Дополнительные ресурсы

- [OWASP Top 10](https://owasp.org/www-project-top-ten/) - основные уязвимости
- [PHP Security Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/PHP_Configuration_Cheat_Sheet.html)
- Документация Timeweb по безопасности

---

## 💡 Итоговые рекомендации

**Для вашего случая (один пользователь):**

1. **Размещение:** Отдельный поддомен `admin.beauty-clinic.ru` (Вариант 1)
2. **Защита:**
   - Авторизация (логин/пароль) - обязательно
   - Хеширование паролей - обязательно
   - HTTPS - обязательно
   - Ограничение по IP - опционально (если IP статичный)
3. **Backend:** PHP (проще для Timeweb)
4. **Интеграция:** Генерация статических файлов через FTP/SFTP

**Это обеспечит:**
- ✅ Безопасный доступ только для вас
- ✅ Изоляцию от основного сайта
- ✅ Простоту использования
- ✅ Легкость поддержки

