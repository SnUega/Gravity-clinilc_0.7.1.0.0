# 🚀 Рекомендации по реализации ключевых компонентов

## 📋 Содержание

1. [Страница "Сайт в разработке"](#страница-сайт-в-разработке)
2. [Страницы блога и услуг](#страницы-блога-и-услуг)
3. [Админ-панель](#админ-панель)

---

## 🎨 Страница "Сайт в разработке"

### Технические рекомендации

#### Вариант 1: WebGL (Рекомендуется для максимальной производительности)

**Библиотеки:**
- **Three.js** - для 3D волн и сложных эффектов
- **GSAP** (уже используется) - для плавной анимации и контроля таймлайна
- **Shader Material** - для градиентных волн
- **CSS backdrop-filter** - для эффекта Apple Liquid Glass

**Преимущества:**
- Высокая производительность (GPU-ускорение)
- Плавная анимация даже на слабых устройствах
- Возможность создания сложных эффектов
- Хорошая интеграция с GSAP

**Реализация:**
```javascript
// Структура файла: js/under-construction.js
// Использовать:
// - Three.js для создания волновой поверхности
// - GSAP для анимации параметров волн
// - Fragment shader для градиентных переходов
// - CSS backdrop-filter для эффекта жидкого стекла
```

### Дополнительные требования

#### 1. Эффект Apple Liquid Glass (iOS 23+)

**Технологии:**
- **CSS `backdrop-filter: blur()`** - основной эффект размытия
- **CSS `backdrop-filter: saturate()`** - насыщенность
- **GSAP** - для анимации скольжения элементов
- **CSS `clip-path`** или **SVG masks** - для формы элементов

**Реализация:**
```css
.liquid-glass-element {
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(20px) saturate(180%);
  -webkit-backdrop-filter: blur(20px) saturate(180%);
  border: 1px solid rgba(255, 255, 255, 0.2);
  border-radius: 20px;
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
}
```

**Анимация скольжения:**
- Использовать GSAP для плавного движения элементов
- Эффект преломления через градиенты и тени
- Параллакс эффект при скролле/движении мыши

#### 2. Интеграция SONLINE для записи

**Требования:**
- Кнопка записи через WhatsApp (обязательно через SONLINE)
- Возможно кнопка записи через Telegram
- Интерфейс выбора способа связи
- Интеграция с API SONLINE для синхронизации расписания

**Реализация:**
```javascript
// js/sonline-integration.js
// Интеграция с SONLINE API
// Кнопки записи с переходом на WhatsApp/Telegram
// Синхронизация расписания
```

**Структура:**
```
html/
└── under-construction.html
    ├── Анимированные волны (WebGL)
    ├── Элементы с Liquid Glass эффектом
    └── Блок записи через SONLINE
        ├── Кнопка "Записаться через WhatsApp"
        └── Кнопка "Записаться через Telegram"
```

#### Вариант 2: Canvas 2D + JavaScript (Проще, но менее производительно)

**Библиотеки:**
- **GSAP** - для анимации
- **Canvas API** - для отрисовки волн
- Возможно: **Paper.js** или **PixiJS** для упрощения

**Преимущества:**
- Проще в реализации
- Меньше зависимостей
- Достаточно для простых волн

**Недостатки:**
- Может быть медленнее на слабых устройствах
- Ограниченные возможности для сложных эффектов

#### Вариант 3: CSS + JavaScript (Самый простой, но ограниченный)

**Технологии:**
- **CSS gradients** + **clip-path** для формы волн
- **GSAP** для анимации transform и clip-path
- **SVG paths** для плавных кривых

**Преимущества:**
- Минимум зависимостей
- Легко интегрировать
- Хорошая производительность для простых волн

### Рекомендация: **Вариант 1 (WebGL + Three.js)**

**Причины:**
1. Уже используется GSAP в проекте
2. Максимальная производительность
3. Возможность создания эффекта, максимально близкого к PSP 3008
4. Хорошая масштабируемость

**Структура файлов:**
```
js/
├── under-construction.js      # Основной контроллер
└── wave-animation.js          # Логика волн (WebGL/Three.js)

css/modules/components/
└── _under-construction.css    # Стили страницы
```

**Ключевые моменты:**
- Монохромная палитра: черный фон (#000), серые волны (rgba от #111 до #ccc)
- Плавные градиенты между волнами
- Несколько слоев волн для глубины
- Анимация через GSAP для синхронизации с остальным проектом

---

## 📄 Страницы блога и услуг

### Архитектурные рекомендации

#### 1. Переиспользование компонентов

**Создать общие компоненты:**
```
components/
├── hero-section/              # Переиспользуемый hero
│   ├── hero.html             # Шаблон
│   ├── hero.js               # Логика
│   └── _hero.css             # Стили
├── service-list/              # Список услуг
└── article-grid/              # Сетка статей
```

**Преимущества:**
- Единообразие дизайна
- Легче поддерживать
- Быстрее разработка новых страниц

#### 2. Структура страниц услуг

**Рекомендуемая структура:**
```html
<!-- html/cosmetology.html -->
<header>...</header>  <!-- Переиспользуемый -->
<main>
  <section class="service-hero">
    <!-- Фиксированная слева на десктопе -->
    <div class="hero-content">
      <h1>Косметология</h1>
      <p>Художественный текст...</p>
      <!-- Подвиды услуг (опционально) -->
    </div>
  </section>
  
  <section class="service-list-container">
    <!-- Прокручиваемый список справа -->
    <div class="service-list">
      <!-- Динамически генерируется из данных -->
    </div>
  </section>
</main>
<footer>...</footer>  <!-- Переиспользуемый -->
```

**CSS подход:**
```css
/* Для десктопа/планшетов горизонтально */
@media (min-width: 1024px) {
  .service-hero {
    position: fixed;
    left: 0;
    width: 40%;
    height: 100vh;
  }
  
  .service-list-container {
    margin-left: 40%;
    width: 60%;
  }
}
```

#### 3. Страница блога

**Рекомендуемая структура:**
```html
<!-- html/blog.html -->
<header>...</header>
<main>
  <section class="blog-hero">
    <!-- Титульная статья (последняя) -->
    <article class="featured-article">...</article>
  </section>
  
  <section class="blog-filters">
    <!-- Поиск + теги -->
    <input type="search" id="blog-search">
    <div class="filter-tags">
      <!-- Динамически генерируются из статей -->
    </div>
  </section>
  
  <section class="blog-grid">
    <!-- Сетка статей -->
    <!-- Фильтруется через JavaScript -->
  </section>
</main>
<footer>...</footer>
```

**JavaScript для фильтрации:**
```javascript
// js/blog-filter.js
// Использовать:
// - Делегирование событий для тегов
// - Debounce для поиска
// - Анимация появления/скрытия через GSAP
// - Хранение данных статей в JSON или через API
```

#### 4. Модальные окна vs Отдельные страницы

**Рекомендация: Модальные окна для услуг, отдельные страницы для статей**

**Услуги (модальные окна):**
- ✅ Быстрее загрузка
- ✅ Не теряется контекст (пользователь остается на странице категории)
- ✅ Легче реализовать с существующей системой модалок
- ✅ Можно использовать GSAP для анимации открытия

**Статьи (отдельные страницы):**
- ✅ Лучше для SEO
- ✅ Можно поделиться ссылкой
- ✅ История браузера работает корректно
- ✅ Автоматическая генерация через админ-панель

**Реализация модальных окон услуг:**
```javascript
// Использовать существующий modal-win.js
// Расширить для поддержки контента услуг:
// - Показания
// - Уход и реабилитация
// - Советы врача
// - Отзывы (динамически по тегам)
// - Статьи (динамически по тегам)
// - Цены
```

#### 5. Данные и структура

**Рекомендуемый формат данных:**
```javascript
// data/services.json
{
  "cosmetology": [
    {
      "id": "dermatonia",
      "title": "Дерматония и БМС",
      "description": "...",
      "indications": [...],
      "care": [...],
      "advice": [...],
      "price": "...",
      "tags": ["косметология", "аппаратная"]
    }
  ]
}

// data/articles.json
{
  "articles": [
    {
      "id": "article-1",
      "title": "...",
      "content": "...",
      "thumbnail": "...",
      "tags": ["косметология", "уход"],
      "relatedServices": ["dermatonia", "bms"],
      "published": true,
      "date": "2025-01-10"
    }
  ]
}
```

**Преимущества JSON:**
- Легко редактировать
- Можно заменить на API позже
- Простая интеграция с админ-панелью

---

## ⚙️ Админ-панель

### Архитектурные рекомендации

#### Вариант 1: Отдельное приложение (Рекомендуется)

**Структура:**
```
admin/
├── index.html                 # Главная страница админки
├── login.html                 # Авторизация
├── css/
│   └── admin.css              # Стили админки
├── js/
│   ├── admin-api.js           # API клиент
│   ├── review-moderation.js   # Модерация отзывов
│   └── article-editor.js      # Редактор статей
└── api/                       # Backend (PHP/Node.js)
    ├── auth.php
    ├── reviews.php
    └── articles.php
```

**Преимущества:**
- Изолированная безопасность
- Не влияет на основной сайт
- Легче масштабировать
- Можно использовать готовые решения (React Admin, Vue Admin)

#### Вариант 2: Интеграция в основной сайт

**Структура:**
```
html/
└── admin/
    ├── index.html
    ├── reviews.html
    └── articles.html

js/
├── admin/
│   ├── auth.js
│   ├── review-moderation.js
│   └── article-editor.js
```

**Преимущества:**
- Единая кодовая база
- Переиспользование компонентов
- Проще для небольших проектов

### Рекомендация: **Вариант 1 (Отдельное приложение)**

**Причины:**
1. Безопасность (изоляция от публичной части)
2. Можно использовать готовые админ-фреймворки
3. Не засоряет основной код
4. Легче обновлять независимо

### Технологический стек для админ-панели

#### Frontend (Рекомендуется)

**⚠️ ВАЖНО: Выбор технологии зависит от опыта разработчика**

**Вариант A: Vanilla JS + существующие библиотеки** ⭐ **РЕКОМЕНДУЕТСЯ**
- Использовать GSAP для анимаций интерфейса
- Простой и легкий
- Интеграция с существующим кодом
- **Не требует изучения новых фреймворков**
- **Можно делать с помощью AI-ассистента (Cursor)**
- Подходит для разработчиков без опыта в React/Vue

**Вариант B: Готовые админ-панели (CMS)**
- **Strapi** (Node.js, headless CMS)
- **Directus** (любой backend, headless CMS)
- **Forest Admin** (готовое решение, платное)
- **Retool** (low-code платформа)
- **Преимущества:** Готовая авторизация, CRUD, файлы, API
- **Недостатки:** Меньше кастомизации, может быть избыточно

**Вариант C: Легкий фреймворк**
- **Vue.js** (проще чем React для новичков)
- **React** (требует изучения JSX, хуков, состояния)
- Готовые компоненты для форм
- Лучшая структура для сложных интерфейсов
- **⚠️ React сложен для новичков:** JSX, хуки, состояние, компоненты
- **⚠️ С Vue.js проще начать**, но все равно нужна база

**Вариант D: PHP + готовые библиотеки**
- **AdminLTE** (готовый шаблон админки на Bootstrap)
- **Tabler** (современный админ-шаблон)
- **Backpack for Laravel** (если использовать Laravel)
- Проще для PHP-разработчиков
- Много готовых решений

**Рекомендация:**
1. **Если нет опыта в React/Vue:** Вариант A (Vanilla JS) или Вариант B (готовые CMS)
2. **Если есть опыт в PHP:** Вариант D (AdminLTE/Tabler)
3. **Если хотите изучить фреймворк:** Вариант C (Vue.js проще чем React)
4. **Если нужна максимальная скорость:** Вариант B (Strapi/Directus)

**Для вашего случая (нет опыта в React):**
- ✅ **Вариант A (Vanilla JS)** - можно делать с помощью AI, используя существующий стек
- ✅ **Вариант B (Strapi/Directus)** - готовое решение, минимум кода
- ❌ **Вариант C (React)** - не рекомендуется без опыта, будет сложно даже с AI

#### Backend

**Варианты:**
1. **PHP** (простой, работает на большинстве хостингов)
2. **Node.js** (если уже используется)
3. **Python Flask/Django** (если нужна сложная логика)

**Рекомендация: PHP** (проще для Timeweb, много готовых решений)

### Функциональные модули

#### 1. Модерация отзывов

**⚠️ Текущий статус:** Нет формы для отправки отзывов на сайте

**Требуется реализовать:**
1. **Кнопка/форма для отправки отзывов** на сайте
   - Модальное окно с формой
   - Поля: имя, текст отзыва, фото (опционально)
   - Кнопка "Отправить на модерацию"
   - Валидация формы

2. **Интерфейс модерации в админ-панели:**
```
┌─────────────────────────────────┐
│ Ожидают модерации (5)          │
├─────────────────────────────────┤
│ [Отзыв 1] [Редактировать] [✓] │
│ [Отзыв 2] [Редактировать] [✓] │
└─────────────────────────────────┘
```

**Функционал админ-панели:**
- Список отзывов на модерации
- Редактор текста (WYSIWYG или простой textarea)
- Загрузка/редактирование фотографий
- Добавление тегов (мультиселект)
- Предпросмотр как будет выглядеть на сайте
- Кнопка "Опубликовать"

**Технические детали:**
- Хранение в БД (MySQL/PostgreSQL)
- API endpoints для CRUD операций
- Валидация данных
- Автоматическое размещение по тегам
- **Интеграция формы на сайте с API админ-панели**

**Рекомендация по форме отзывов:**
- Добавить кнопку в секцию ALR (отзывы)
- Модальное окно с формой (использовать существующий `modal-win.js`)
- Отправка на `/api/reviews/submit` (ожидает модерации)
- Уведомление пользователю о том, что отзыв отправлен на модерацию

#### 2. Редактор статей

**Интерфейс:**
```
┌─────────────────────────────────┐
│ Редактор статьи                 │
├─────────────────────────────────┤
│ Заголовок: [____________]       │
│ Превью: [____________]          │
│                                   │
│ [WYSIWYG Editor]                 │
│                                   │
│ Теги: [✓ косметология] [✓ уход] │
│ Связанные услуги: [мультиселект]│
│                                   │
│ [Загрузить изображения]          │
│                                   │
│ [Предпросмотр] [Опубликовать]   │
└─────────────────────────────────┘
```

**Функционал:**
- WYSIWYG редактор (TinyMCE, CKEditor или простой Markdown)
- Загрузка изображений с превью
- Управление тегами
- Связывание с услугами
- Автоматическая генерация страницы
- **Автоматическое обновление hero блога** (последняя статья)
- **Автоматическое добавление в сетку блога** (вытесняя старые)
- **Автоматическое обновление мини-блога в меню** (7 последних статей)
- Автоматическое проставление ссылок

**Логика размещения статей:**
1. Новая статья → становится титульной в hero блога
2. Предыдущая титульная → перемещается в сетку блога
3. Мини-блог в меню → обновляется до 7 последних статей (вытесняя старые)
4. Создается страница статьи → `html/blog/articles/article-slug.html`

**Технические детали:**
- Генерация HTML страниц на сервере
- Создание файлов в `html/blog/articles/`
- Обновление `data/articles.json` или БД
- Автоматическое создание sitemap

### Безопасность

**Обязательные меры:**
1. **Авторизация:**
   - JWT токены или сессии
   - Хеширование паролей (bcrypt)
   - Защита от CSRF

2. **Валидация:**
   - Проверка всех входных данных
   - Санитизация HTML контента
   - Проверка типов файлов для загрузки

3. **Ограничения:**
   - Rate limiting для API
   - Логирование действий админа
   - Резервное копирование данных

### Интеграция с основным сайтом

**Подход: Генерация статических файлов на хостинге**

**Как это работает:**
1. Админ-панель (отдельное приложение) подключается к хостингу через FTP/SFTP или API
2. При публикации статьи админ-панель:
   - Генерирует HTML файл страницы статьи
   - Обновляет JSON файлы данных (`data/articles.json`)
   - Обновляет файлы для мини-блога (`data/menu-blog.json`)
   - Загружает файлы на хостинг через FTP/SFTP API
3. Основной сайт читает обновленные файлы

**Структура данных:**
```javascript
// data/articles.json (обновляется админ-панелью)
{
  "articles": [
    {
      "id": "article-1",
      "title": "...",
      "slug": "article-slug",
      "content": "...",
      "published": true,
      "date": "2025-01-10",
      "order": 1  // Последняя = order: 1
    }
  ]
}

// data/menu-blog.json (7 последних статей для меню)
{
  "articles": [
    // Только 7 последних, отсортированных по дате
  ]
}
```

**Процесс публикации:**
```
Админ публикует статью
    ↓
Админ-панель генерирует:
    ├── html/blog/articles/article-slug.html
    ├── Обновляет data/articles.json
    ├── Обновляет data/menu-blog.json (7 последних)
    └── Загружает на хостинг через FTP/SFTP
    ↓
Основной сайт автоматически отображает новую статью
```

**Технические детали:**
- Использовать библиотеки для FTP: `node-ftp`, `ssh2-sftp-client` (Node.js) или `phpseclib` (PHP)
- Или использовать API хостинга (если доступно)
- Возможно использование Git для автоматического деплоя

**Альтернативный подход: API + БД**
- Админ-панель записывает в БД
- Основной сайт читает через API
- Более гибко, но требует backend на хостинге

---

## 🎯 Общие рекомендации

### Порядок реализации

1. **Сначала:** Страница "Сайт в разработке" (быстро, визуальный эффект)
2. **Затем:** Переиспользование компонентов (header, footer, форма)
3. **Параллельно:** Страницы услуг + Блог
4. **После:** Админ-панель (нужны данные для тестирования)

### Технологии и библиотеки

**Использовать существующие:**
- ✅ GSAP (уже в проекте) - для всех анимаций
- ✅ Lenis (уже в проекте) - для плавного скролла
- ✅ Модульная CSS структура - расширять, не ломать

**Добавить:**
- ⚠️ Three.js (только для страницы "в разработке")
- ⚠️ WYSIWYG редактор (для админки)
- ⚠️ Backend (PHP/Node.js для админки)

### Производительность

**Оптимизации:**
- Ленивая загрузка изображений
- Code splitting для больших страниц
- Кэширование данных статей/услуг
- Минификация JS/CSS для продакшена

### SEO

**Учесть при разработке:**
- Семантический HTML
- Мета-теги для каждой страницы
- Структурированные данные (Schema.org)
- Правильные заголовки (h1-h6)
- Alt-теги для изображений

---

## 📝 Итоговые рекомендации

### Страница "Сайт в разработке"
✅ **WebGL + Three.js + GSAP**
- Максимальная производительность
- Эффект близкий к PSP 3008
- Интеграция с существующим стеком

### Страницы блога и услуг
✅ **Модульная структура + переиспользование компонентов**
- Hero компонент (переиспользуемый)
- Service list компонент
- Article grid компонент
- Модальные окна для услуг
- Отдельные страницы для статей

### Админ-панель
✅ **Рекомендуемые варианты (в зависимости от опыта):**

**Вариант 1: Vanilla JS + PHP Backend** ⭐ **Для новичков**
- Отдельное приложение
- Изолированная безопасность
- Простая интеграция с Timeweb
- Можно делать с помощью AI-ассистента
- Не требует изучения React/Vue

**Вариант 2: Strapi/Directus (Headless CMS)**
- Готовое решение, минимум кода
- Автоматическая авторизация, CRUD, файлы
- API из коробки
- Быстрая разработка

**Вариант 3: PHP + AdminLTE/Tabler**
- Готовые шаблоны админки
- Проще для PHP-разработчиков
- Много готовых компонентов

**Не рекомендуется: React** (без опыта будет сложно даже с AI)

---

## 📌 Дополнительные уточнения

### Статьи: Логика размещения

**При публикации новой статьи:**

1. **Hero блога:**
   - Новая статья становится титульной (последняя)
   - Предыдущая титульная перемещается в сетку

2. **Сетка блога:**
   - Новая статья добавляется в начало сетки
   - Старые статьи сдвигаются вниз

3. **Мини-блог в меню:**
   - Обновляется до 7 последних статей
   - Старые статьи вытесняются
   - Используется в `blog-manager.js` (уже есть логика)

**Реализация:**
```javascript
// При публикации статьи
function publishArticle(article) {
  // 1. Обновить data/articles.json (добавить в начало)
  // 2. Обновить data/menu-blog.json (7 последних)
  // 3. Создать html/blog/articles/{slug}.html
  // 4. Обновить hero блога (последняя статья)
  // 5. Обновить сетку блога
  // 6. Обновить мини-блог в меню
}
```

### Отзывы: Форма отправки

**⚠️ Текущий статус:** Нет формы для отправки отзывов на сайте

**Требуется реализовать:**

1. **Кнопка на сайте:**
   - В секции ALR (отзывы) - рекомендуется
   - Или в футере
   - Открывает модальное окно

2. **Модальное окно с формой:**
   - Имя пользователя
   - Текст отзыва
   - Загрузка фото (опционально)
   - Кнопка "Отправить на модерацию"

3. **Обработка:**
   - Отправка на `/api/reviews/submit`
   - Статус: "ожидает модерации"
   - Уведомление пользователю

**Использовать существующие компоненты:**
- `modal-win.js` для модального окна
- Форма аналогична форме контактов
- Валидация перед отправкой

### Админ-панель: Интеграция с хостингом

**Механизм работы:**

1. **Админ-панель (отдельное приложение):**
   - Работает локально или на отдельном поддомене
   - Подключается к хостингу через FTP/SFTP

2. **При публикации:**
   - Генерирует статические файлы (HTML, JSON)
   - Загружает на хостинг через FTP/SFTP API
   - Обновляет существующие файлы

3. **Основной сайт:**
   - Читает обновленные JSON файлы
   - Отображает новый контент

**Библиотеки для FTP/SFTP:**
- **Node.js:** `node-ftp`, `ssh2-sftp-client`
- **PHP:** `phpseclib`, встроенный `ftp_*` функции

**Альтернатива: Git + автоматический деплой:**
- Админ-панель коммитит в Git
- Автоматический деплой на хостинг (через webhook)
- Более современный подход

### Страница "Сайт в разработке": Дополнительные элементы

**1. Эффект Apple Liquid Glass (iOS 23+):**

**CSS реализация:**
```css
.liquid-glass {
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(20px) saturate(180%);
  -webkit-backdrop-filter: blur(20px) saturate(180%);
  border: 1px solid rgba(255, 255, 255, 0.2);
  border-radius: 20px;
  box-shadow: 
    0 8px 32px rgba(0, 0, 0, 0.1),
    inset 0 1px 0 rgba(255, 255, 255, 0.3);
}

/* Анимация скольжения */
@keyframes liquid-slide {
  from {
    transform: translateX(-100%) translateY(0);
    opacity: 0;
  }
  to {
    transform: translateX(0) translateY(0);
    opacity: 1;
  }
}
```

**GSAP анимация:**
- Плавное появление элементов
- Эффект скольжения при скролле
- Параллакс при движении мыши
- Преломление фона через градиенты и тени

**2. Интеграция SONLINE:**

**Кнопки записи:**
- "Записаться через WhatsApp" (через SONLINE API) - **обязательно**
- "Записаться через Telegram" (опционально)
- Интерфейс выбора способа связи

**Технические детали:**
- Интеграция с SONLINE API
- Синхронизация расписания
- Переход на WhatsApp/Telegram с предзаполненными данными
- **Клиника уже работает, поэтому функционал записи критичен**

**Следующие шаги:**
1. Создать структуру компонентов
2. Реализовать страницу "в разработке" (с SONLINE и Liquid Glass)
3. Добавить форму отправки отзывов на сайте
4. Разработать шаблоны страниц услуг
5. Создать систему данных (JSON → API позже)
6. Разработать админ-панель с FTP/SFTP интеграцией

